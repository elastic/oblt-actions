name: pre-commit

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install gh-action-readme
        run: gh extension install actions/gh-action-readme --pin v0.2.0
      - uses: actions/setup-python@v5
      - uses: pre-commit/action@v3.0.1
