name: Observability Agentic Workflow — Security Detector

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-aw:
    permissions:
      actions: read
      contents: read
      id-token: write
      issues: read
      pull-requests: read
    uses: elastic/oblt-aw/.github/workflows/oblt-aw-security-detector.yml@main # ratchet:exclude
