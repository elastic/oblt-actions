name: Observability Agentic Workflow — Issue Triage

on:
  issues:
    types: [opened]

permissions:
  contents: read

jobs:
  run-aw:
    permissions:
      actions: read
      contents: read
      discussions: write
      issues: write
      pull-requests: write
    uses: elastic/oblt-aw/.github/workflows/oblt-aw-issue-triage.yml@main # ratchet:exclude
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
